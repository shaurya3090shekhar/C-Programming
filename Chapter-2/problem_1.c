//To find the volume of a sphere,radius is given
#include<stdio.h>
int main()
{
    float r = 9;
    float volume = (4*3.14*r*r*r)/3;
    printf("The volume of a given sphere is: %f",volume);

    return 0;
}