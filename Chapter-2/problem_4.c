//WAP to calculate the Simple Intrest of a given data
#include<stdio.h>
int main()
{
    float p = 686000;
    float r = 11.15;
    float t = 2;
    float SI = (p*r*t)/100;
    printf("%f",SI);

    return 0;
}